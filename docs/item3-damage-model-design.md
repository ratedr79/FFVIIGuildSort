# Item 3 — Scoring rebuild on the real damage model (design for review)

Goal: replace the team builder's **weighted-sum heuristic** with an **estimated-team-damage** score grounded in the real multiplicative formula (the one already in `DamageCalcService` and `Shira_Damage_Calc.xlsx`) and real percentages. This makes cross-family multiplication, breakpoints, %-magnitudes, dedup, and phase-gating *emergent* instead of hand-tuned.

This is a review draft. Nothing here is committed; annotate freely. Decisions needing your sign-off are marked **[DECISION]**.

---

## 1. The scoring core — `EstimateTeamDamage(team, request)`

For a candidate team (3 characters, each with main/off/ultimate/outfit/3 subs/2 sub-costumes/materia), produce one number: estimated team damage.

**Step A — build the team buff/debuff state.** Collect every detected effect across all characters' equipped slots + the assumed-materia baseline, grouped by **family** (= one `(1+X)` term in the formula). Within a family, sum the real %s (from the tier→% tables) up to the cap; across families they stay separate factors.
- Ally buffs (PATK/MATK Up, elemental damage up, weapon boost, damage bonus, amplification, exploit weakness): a team-wide (All-Allies) source applies to every attacker; a self source applies only to its owner.
- Enemy debuffs (PDEF/MDEF Down, elemental resist down, damage-received-up, enfeeble): apply to the target → benefit every attacker.
- Apply **amplifiers** (Enliven / Applied-Stat-Tier-Increase) AFTER the base state: +2 tiers up the same table on the families present; **0 if the family is empty**.
- Apply **uptime** per family (see §3) to scale its %.

**Step B — estimate each attacker's damage.** For each damage-dealing character:
`dmg ≈ weapon% × baseConst × ∏ over families ( 1 + teamState[family]% that applies to this attacker ) × (1 − enemyResist) / enemyDEF(after debuffs)`
using on-element / on-axis terms only, mirroring `DamageCalcService`'s physical/magical branches.

**Step C — team score = Σ attackers' estimated damage.** Supports deal ~0 of their own but raised the attackers' factors in Step A, so their value is *emergent* (the increase they cause to the carry's product). This is the cross-character dependency, handled for free.

**[DECISION] Carry weighting:** sum all attackers' damage equally, or weight the primary carry higher (since a real fight funnels casts to the carry)? Leaning: weight by an assumed cast share (carry ~60%, secondary ~30%, support ~10%), configurable.

---

## 2. Effect → formula-term mapping (additive within family / multiplicative across)

| Family (one `(1+X)` term) | Effects that ADD into it | Source of % |
|---|---|---|
| attack_buff | PATK Up / MATK Up (on-axis) | active tier→% (10/20/30/40/50) |
| damage_up | Elemental Damage Up | active tier→% (10/25/40/60/80) |
| ability_potency (base block) | Boost Ability Pot, Phys/Mag Ability Pot, Element Pot, all-allies variants | passive breakpoint tables |
| defense_debuff (enemy DEF) | PDEF/MDEF Down | active tier→% (15/25/35/45/55) → DEF denominator |
| elemental_resistance_debuff | Elem. Resist Down | active tier→% (15/30/50/75/100) |
| exploit_weakness | Exploit Weakness | explicit % (30/40/60…) |
| damage_bonus | Phys/Mag/Elem Damage Bonus | explicit % |
| weapon_boost | Phys/Mag/Elem Weapon Boost | explicit % |
| ability_amplification | Amp. Phys/Mag/Elem Abilities | explicit % × count-uptime |
| damage_received_up | single/multi-tgt Phys/Mag/Elem Dmg Rcvd Up | explicit % (single-tgt N/A to AOE attackers) |
| torpor | Torpor | explicit % |
| amplifier (modifier, not a term) | Enliven, Applied Stat Buff/Debuff Tier Increase | +2 tiers on attack_buff / debuffs |

---

## 3. The four new mechanics

**3a. Amplifiers as dependency multipliers.** After Step A's base state, for each amplifier present, move the matching family up the tier table by its tier count (≈+2), capped at the table max; contributes nothing if that family is empty. (Replaces the current flat amplifier base + crude synergy bonuses.)

**3b. Uptime / maintenance.** Each family gets an uptime fraction in [0,1] that scales its %:
- A buff/debuff carried on a character's **always-cast main attacking weapon** ≈ full uptime (auto-maintained every turn).
- Otherwise uptime ≈ min(1, (baseDuration + extensions × castsPerWindow) / windowLength), improved when **multiple sources extend the same family**, and bounded by whether a **maintainer** has the spare casts/ATB.
- **[DECISION] Fidelity:** start simple — full uptime for main-weapon-carried families, a fixed high fraction (~0.85) for families a dedicated maintainer covers, lower for one-off/non-reapplied (Lucia-style start-only) effects. Refine later. Agree?

**3c. Charge-time usability.** Discount effects gated behind a long initial charge (Conformer 20s) vs turn-one usable (Rising Sun 0s). Partially modeled today; make it first-class as an uptime/availability factor.

**3d. Cross-character dependency.** Emergent from Step A (everyone's buffs merge into shared family terms), so e.g. Ragnarok's start-at-PATK-High + Aerith's PATK extension fill+maintain the attack_buff term without special cases.

---

## 4. Assembly / search (keep structure, swap the score)

The combinatorial search (team combos × variants × subs) stays — it's the runtime cost, and the formula is cheap, so swapping the scoring function shouldn't change runtime materially. The carry-first / missing-layer logic becomes *emergent* from maximizing estimated team damage:
- Highest-%, on-element, on-axis weapon naturally becomes the carry (it dominates the damage sum).
- A support is chosen when it raises the carry's product most (fills an empty/under-filled family) — the marginal-team-damage gain, which is your "compute the marginal %" rule generalized.
- Breakpoint stacking falls out (passive %s have diminishing table steps; the optimizer stops when marginal team-damage is small).
- "Don't compromise the carry for defense" is automatic — defense isn't in the offensive product, so it only wins a slot when offense is saturated or defense is requested.

**[DECISION] Scope of the swap:** replace only the per-character + team *offensive* score with the damage estimate, leaving the defensive/role/template machinery as-is? Or fuller rewrite? Leaning: targeted swap of the offensive scoring core first, to bound blast radius.

---

## 5. What's held constant (cancels in ranking)

Materia (beyond the assumed baseline), memoria, summons, guild bonuses, overspeed, base PATK/MATK: unknown and roughly constant per character across candidates, so they're held fixed and **cancel out when ranking builds**. We estimate "damage we control," not absolute damage — fine for choosing between builds.

**Out-of-scope team mechanics (user, not in analyzer / not planned now):**
- **Memoria** — a SINGLE team-wide selection (one per team). Adds base stats to every character (PATK/MATK/PDEF/MDEF/heal) + some abilities (e.g. Fire Pot Up, +dmg vs certain species). `data/memoria.json` lists names + partial abilities. Held constant.
- **Summon slot** (per character, ATB-FREE; a gauge fills as you act, threshold varies by choice). Holds one of: **limit breaks** (3 per character, distinct buffs/debuffs/effects/damage), **enemy skills** (shared pool, one per team, distinct buffs/debuffs/statuses), or **summons** (one per element + some double-summons with special effects like element-damage-up or an amp). Held constant.
- **Why off-axis buffs get a SMALL flexibility value (not zero):** the main elemental summons deal phys/mag elemental damage computed from the **AVERAGE of the character's PATK and MATK stats** (+ buffs) — a formula likely visible in the damage calc. So in a physical fight a weapon's MATK Up still helps the MATK half of that average **during summon usage** — slight, but real. This justifies `OffAxisBuffFlexibilityFactor` being small; it is NOT a tuning hack. (If anything it could trend even smaller, since summons are occasional and only half the average benefits.)

---

## 6. Phasing & risk

This replaces the scoring core → broad test re-baselining (expected; the snapshot tests are already skipped pending exactly this). Suggested phases:
1. Build `EstimateTeamDamage` behind a flag; unit-test it against the spreadsheet's worked examples (rows 51–71) and your phys-lightning walkthrough end-state.
2. Switch the offensive team score to it; re-baseline characterization tests to the new (correct) shells.
3. Fold in uptime/charge/amplifier fidelity; re-validate against your walkthrough.

---

## Resolved decisions
- **[D1] Weight the carry.** Carry ~60% / secondary ~30% / support ~10% cast share (configurable).
- **[D2] Start simple (bucketed uptime).** Full uptime for buffs on the always-cast main attacking weapon; ~0.85 for families a dedicated maintainer covers; low for start-only/non-reapplied effects. Refine later.
- **[D3] Offensive core first.** Swap only the offensive scoring core; leave defensive/role/template machinery as-is. Focus on the ONE pattern (Lightning / Physical / Single-Enemy / Adaptive) — get the default right before broadening. (User: defaults haven't matched expectations, and UI options can't be tested until the default does.)
- **[D4] No special-casing weapons/characters — and be armory-fluid.** Keep everything weapon/character-agnostic (effect-family based). The model must degrade gracefully for REAL armories: missing costumes/weapons and weapons at different overboost levels. We've been testing with ALL items; the estimator must value whatever effects a given (possibly partial) build actually provides, with OB feeding the real %s through the snapshot. No assumption that any specific item exists.

## Phase 2 finding (team-score swap experiment)

The damage MODEL (Phase 1) and the variant→inputs extraction glue (`EstimateTeamDamage(baseVariants, request)`) are built and unit-tested. A team-score swap (blend `EstimateTeamDamage` over the legacy score, gated to offensive builds) was tried and **reverted** — it revealed the real bottleneck: **the per-character variant SELECTION is not damage-aware**, so the team-score swap can only choose among already-bad variants. Concrete evidence (phys-lightning shell): Yuffie's main = **Bird of Prey (940%)** with **Winged Chakram (1340%) in the off-hand** — the variant composition parks the *lower*-damage weapon in main, because main/off orientation is decided by R-abilities, not damage. Aerith's main = Umbrella (heal) similarly.

**Element nuance (user correction):** Bird of Prey is 940% **Water**, Winged Chakram is 1340% **Lightning**. In a Lightning battle the off-element weapon gets NONE of the weakness-exploit / resist-down / element-damage-up terms (and may be resisted), so its *effective* damage is far below 940 — Winged Chakram should win decisively. Having the main *damage* weapon in the off-hand isn't categorically wrong (actives fire from both slots) but is uncommon and wrong here because it sidelines the on-element carry weapon. **Gap to fix:** `GetVariantWeaponDamagePercent` currently uses the RAW `DamagePercent`; it must be **element/axis-gated** — an off-element / off-axis weapon's effective % is heavily discounted (no weakness/element terms apply to it), and the per-attacker estimate should apply only the buff families that match that weapon's element/axis.

So the true Phase 2 is **damage-aware variant selection**, which has two parts and is architecturally deep:
1. **Main/off orientation by damage** — ✅ **DONE (verified, zero blast radius).** `GetWeaponEffectiveDamagePercent` (element/axis-gated) + the swap block in `BuildCharacterVariants`: when either weapon is a substantial attacker (effective % > `AttackingWeaponDamageThreshold` = 500, offensive build), orient PURELY by effective damage and never let the R-ability comparison demote it. **Subtlety that took two tries:** the swap loop iterates *both* orderings of a pair, so a threshold rule alone wasn't enough — the R-ability comparison on the reverse pair (`main=Winged, off=Bird`) flipped Winged Chakram back *out* of main (Bird's passives score higher there). The fix gates the *entire* swap decision (not just an extra swap reason). Result: Yuffie main = Winged Chakram 1340% Lightning; Bird of Prey (Water) demoted to a sub. Full suite: 236 pass / 1 pre-existing fail / 7 skip — no regressions. Pure support weapons (both below threshold) keep R-ability orientation.
2. **Variant ranking by damage contribution** (still TODO): `GetVariantSelectionScore` should rank a character's builds by their contribution to estimated team damage. The seam: variants are composed and pruned to top-N *before* team context exists, so a per-character damage proxy (carry maximizes own output; support value is team-buff contribution to the carry) is needed, or the pool must keep enough diversity for a team-level pass to choose. This is the same architectural seam as team-wide dedup.

Remaining Phase 2: (2) above; Aerith-style support-main R-ability orientation (Umbrella heal currently mains for the support — lower priority, role-correct-ish); re-apply the team-score swap on the now-correct variant pool, calibrate; replace the brittle snapshot tests with property/ranking assertions.

### Phase 2.3 attempt 2 — team-score swap on the corrected pool (tried, REVERTED again — now with concrete ordering evidence)

Re-applied the blend (`score = EstimateTeamDamage + legacy × 0.15`, offensive only) on the post-2.1 pool. **Good news:** it selected the RIGHT team — Yuffie/Cloud/Aerith with Winged Chakram as Yuffie's carry main and Abraxas (debuff amp) on Aerith. **But it regressed 4 tests**, all from the damage model being incomplete (so de-weighting the legacy score to 0.15 dropped refinements the estimate doesn't yet replicate):
- `MagicalFireKaiserTeamBeatsRawAlternative` — model scored the raw-Tifa team (2299) ABOVE the Kaiser team (2252); Kaiser should win. → the estimate is missing the value that makes Kaiser better (**uptime / coverage breadth**).
- `ExhaustiveMode_SelectsFestiveRodForAerith` — Festive Rod no longer chosen for Aerith. → **support-buff valuation** not yet in the estimate (a support's main-slot All-Allies buff value).
- `DoesNotKeepMermaidRodAndKamuraTogether` — dedup behaviour lost. → the **team-wide duplicate-buff penalty** isn't folded into the estimate.
- `SelectedTeamDiagnostics` — downstream of the above.

**Phase 2.2a — bucketed uptime: ✅ DONE (8 model tests green, isolated/no live impact).** `ApplyUptimeBuckets` + `MaintainerUptime = 0.85` in `EstimateTeamDamage`: a family carried on an attacker's always-cast MAIN weapon (effective % > threshold) gets full uptime; others ×0.85; passives flat. The baseVariants overload derives the always-cast weapon names from attackers' main weapons and marks their effects' families full-uptime. Unit-tested via the core's new `(fullUptimeFamilies, maintainerUptime)` params (default 1.0 = no-op, preserving prior tests).

**Calibration insight for the swap (scale mismatch):** the legacy penalties/bonuses (dedup penalty, etc.) live on the LEGACY score scale (~tens–hundreds), while `EstimateTeamDamage` is ~1000–4000. So folding a legacy penalty into the damage-primary score by simple subtraction is too weak to bite — the dedup fold (and any preserved legacy refinement) must be **rescaled onto the damage-estimate magnitude**, not just added. This is the core remaining calibration work before the swap can land, alongside support variant ranking (Festive Rod). Expect a few full-suite iterations.

### Phase 2.3 attempt 3 — proper backbone-replace + full-weight refinements + uptime (tried, REVERTED — deeper finding)

Restructured both scoring points to replace ONLY the offensive backbone with `EstimateTeamDamage` (uptime-aware) and keep refinement terms (dedup, requirements, alignment) at FULL weight (the scale-mismatch fix). Result: **WORSE — 7 failures (6 new).** Critically:
- `MagicalFireKaiserTeamBeatsRawAlternative` STILL fails — **uptime did NOT fix Kaiser.**
- New: `CaitTrumpetShellTeamBeatsMarchingHornAlternative_OnTargetBreadthCoverage`, `AerithIncludesKamura_WhenKamuraRousingAndGambanteinnExist`.
- Dedup + Festive snapshots still break (full-weight penalty didn't save dedup — the selected team changes too much).

**Deeper finding (the real blocker):** the swap failing is NOT just refinement calibration — `EstimateTeamDamage` **is not yet a good enough PRIMARY ranking signal.** It fails the **coverage-breadth benchmarks** (Kaiser, Cait Trumpet) where a broader-coverage team should beat a higher-raw-weapon-% team. The model's **coverage-vs-weapon-% balance is off**: the carry-weighted weapon-% term (940 vs 1340 — large spread) dominates the product too much relative to the family-coverage terms (~+30-50% each), so a higher-weapon-% / lower-coverage team out-scores a broader-coverage team. Uptime didn't move Kaiser, so that wasn't the missing piece either. Before the swap can land, the model needs **ranking-quality refinement validated against these benchmarks** (likely: temper the weapon-% spread's dominance and/or strengthen coverage-breadth value; possibly a magical-axis issue since Kaiser is magical-fire). This is investigative, per-benchmark work — NOT a weight tweak. Reverted to the clean 2.1 + 2.2a checkpoint (236 pass / 1 pre-existing / 7 skip). Uptime stays (isolated).

### Phase 2.3 investigation — defense-debuff denominator fix (KAISER FIXED; Cait-Trumpet is a judgment call)

Root-caused the coverage-vs-weapon-% imbalance by checking the formula (not dumping breakdowns): **PDEF/MDEF Down were modeled as ×(1+d) but their real effect is ×1/(1-d)** (they reduce the enemy-defence DENOMINATOR, `/EnemyDEF`). At 35% that's ×1.54 vs ×1.35 — the model was **understating debuff coverage**. Fixed in `EstimateTeamDamage` (only `defense_debuff`; `elemental_resistance_down`/`damage_received_up` stay `1+%` per the formula) + unit test (9 model tests green). The fix is isolated (used only by the swap).

Re-applied the backbone-replace swap to validate: **`MagicalFireKaiserTeamBeatsRawAlternative` now PASSES** — the debuff fix flipped it (the diagnosis was correct). Remaining failures with the swap on (6): 
- **`CaitTrumpetShellTeamBeatsMarchingHorn` — NEAR-TIE (Trumpet 13028.86 vs Marching 13066.96, 0.3%).** The two teams differ only in Cait Sith's weapon: Trumpet Shell = PATK Up **+ MATK Up**, Marching Horn = PATK Up only, in a **Physical/Wind** fight. Trumpet's extra **MATK Up is OFF-AXIS** (worthless for a physical team), so the model correctly gives it ~no value and doesn't rank Trumpet above Marching. This is the OPPOSITE of Kaiser: Kaiser's MDEF Down was real value the model wrongly ignored (fixed); Cait-Trumpet's MATK Up is value the benchmark credits but the model — per the established off-axis principle — correctly does not. **JUDGMENT CALL PENDING (user):** is the benchmark misaligned (off-axis breadth shouldn't win), or should universal/off-axis coverage get some flexibility value?
- `ExhaustiveMode_SelectsFestiveRodForAerith`, `DoesNotKeepMermaidRodAndKamuraTogether`, `AerithIncludesKamura...`, `SelectedTeamDiagnostics` — the brittle exact-shell snapshots (planned for property/ranking rewrite + re-baseline).

Swap reverted again to keep the suite clean (debuff fix retained, dormant). **Net: the debuff fix is a genuine, formula-grounded model-quality win — it resolved the legitimate coverage case (Kaiser).** The swap's remaining blockers are now narrow: one off-axis judgment call + the brittle-snapshot re-baseline.

### Phase 2.3 — off-axis flexibility (user: small flexibility value) + debuff fix re-validation

User chose "small flexibility value" for off-axis buffs (a universal PATK+MATK weapon should edge out a single-axis one). Implemented: off-axis effects no longer dropped — they accumulate (best-per-key) into one small `off_axis_flexibility` term = Σ(off-axis %) × `OffAxisBuffFlexibilityFactor` (0.1). Unit-tested (9 model tests green).

Re-applied the swap to validate (both model fixes live). **Kaiser stays fixed (passes); Cait-Trumpet STILL fails** — and the gap is byte-identical before/after the flexibility term (both teams +506.26 from the debuff fix; flexibility added 0). **Root cause:** the off-axis flexibility isn't connecting in production — the detected MATK Up's `GetActiveEffectRealPercent` returns 0 (the off-axis buff lacks a resolvable tier/%, which the unit test masked by setting the tier manually). The mechanism is correct and unit-tested; the production data path for off-axis buffs needs debugging (likely: the detected off-axis effect carries no `PotencyTierRank`/`PotencyPercent`, or its family/axis keys differ from expectations). **TODO:** instrument `EstimateTeamDamage` to dump the Trumpet team's `off_axis_flexibility` and the MATK Up effect's tier/%/axis to find why it resolves to 0.

Reverted the swap again to keep the suite clean. **Both model fixes stay (isolated, unit-tested, dormant): defense-debuff denominator 1/(1-d) [real win — flips Kaiser] + off-axis flexibility [correct mechanism, needs production-detection debug].** Remaining swap-landing blockers: (1) Cait-Trumpet off-axis detection bug, (2) re-baseline the 4 brittle snapshots (Festive/dedup/Kamura/diagnostics) as property tests.

### Phase 2.3 — Cait-Trumpet RESOLVED: no bug; the benchmark is misaligned (redundant + off-axis)

Diagnosed the "off-axis flexibility didn't connect" puzzle by dumping each character's PATK/MATK labels:
```
TRUMPET:  Tifa[PATK Up] Vincent[MATK Up] Cait Sith[MATK Down, MATK Up, PATK Down, PATK Up]
MARCHING: Tifa[PATK Up] Vincent[MATK Up] Cait Sith[MATK Down, PATK Down, PATK Up]
```
**Vincent provides MATK Up in BOTH teams.** So Trumpet Shell's MATK Up on Cait Sith is REDUNDANT — the team already has MATK Up. The off-axis flexibility term is best-per-key (correct dedup), so the duplicate contributes nothing → the gap was byte-identical. **There is NO bug.** The model correctly treats a redundant buff as redundant (the [[duplicate-buff-dedup]] principle), and it's off-axis besides. So Cait-Trumpet rejecting Trumpet is RIGHT on two counts.

**Conclusion:** Cait-Trumpet is a **misaligned benchmark** — it rewards "broader coverage" that is actually a redundant, off-axis buff. Per the user's "delete tests superseded by model + benchmarks" decision and the dedup principle, it should be **deleted** alongside the 4 brittle snapshots. (Flag for user veto since it's a benchmark, not a snapshot.) This **clears swap-landing blocker 1** (off-axis was never broken). Remaining: delete the 5 misaligned/brittle tests → re-apply swap → validate scenarios → land.

### ⚠️ PHASE 2.2 REGRESSION (surfaced by multi-team groundwork) — amplifier-support exclusion

The multi-team alternates dump (Take(20)) revealed only **6 distinct rosters exist in the whole candidate pool, NONE with Aerith** — so post-2.2 Aerith is not ranked low, she is **not generated as a team candidate at all** (pre-2.2 she was the #1 team). Root cause: Phase 2.2's per-character variant score excludes her from the character pool, and the deep reason is that **Aerith's headline value is Abraxas, a debuff AMPLIFIER worth ~0 when she's evaluated SOLO** (no base debuffs in her own build to amplify — its value only exists in team context alongside Yuffie's PDEF Down). The team-buff proxy lifted her some but cannot credit a team-context-dependent amplifier. Cid (a support who ALSO attacks) survives, so it's not "supports excluded" — it's **amplifier-dependent supports undervalued.** So 2.2 improved Cloud's main but made the user's primary team unreachable — likely a net regression for this scenario. Earlier "Cid barely beats Aerith / close call" framing was WRONG (the model excludes Aerith entirely). Multi-team output earned its keep by surfacing this. **Decision pending (options):** (A) make the team-buff proxy amplifier-aware (credit an amplifier assuming reference base buffs/debuffs); (B) ensure character-pool ROLE DIVERSITY so amplifier-supports aren't dropped pre-team; (C) soften 2.2's variant score (blend with legacy) so supports keep a floor; (D) revert 2.2 (restores Aerith, loses Cloud-main improvement). NOTE: multi-team enrichment itself (full builds + adds/drops rationale) is kept — additive, zero regressions.

### ↩️ PHASE 2.2 + A + B REVERTED (and why) → see "Candidate-generation rework (planned)" below

Phase 2.2 (damage-aware per-character variant ranking) + A (amplifier-aware support credit) + B (support-pool character diversity) were all **reverted** (kept: the damage model, the team-score swap, 2.1 carry orientation, and the additive multi-team groundwork). Reason: 2.2 excluded the user's primary amplifier-support (Aerith) from candidate generation, and A/B couldn't fix it because the exclusion lives in the multi-layer legacy candidate pipeline (seed pool → `ScoreSupportSeedForAnchor` gate), where a per-character score structurally cannot value team-context things (amplifiers, All-Allies buffs). Patching each layer was whack-a-mole. **Post-revert verified:** BEST = Yuffie/Cloud/Aerith (Aerith back, Festive Rod + Abraxas), Yuffie carry = Winged Chakram (2.1 kept), Cloud main = Noble Parasol (the minor "feels off" returns — the accepted cost), multi-team alternates populate, runtime ~4.5s, suite green. The detail below (PHASE 2.2 LANDED) is **historical** — superseded by the revert.

### 🔭 Candidate-generation rework (PLANNED — the correct future state)

**Problem statement.** The legacy candidate pipeline uses **per-character scores to GATE which characters become team candidates** (seed pool order, anchor selection, `ScoreSupportSeedForAnchor`, support-pool `Take`). A per-character score is computed on a build *in isolation*, so it cannot see team context — it undervalues amplifiers (worth ~0 with no base to amplify), All-Allies buffs (valued vs the support's own low weapon %), and any cross-character synergy. So team-dependent characters get filtered out *before* the team scorer — which IS correct — can judge them. (Aerith was the concrete casualty.)

**The principle (a simplification, not more heuristics).** `EstimateTeamDamage(team) + refinements` (the landed team scorer) already values everything in context. So: **stop using per-character scores to EXCLUDE characters.** Per-character scoring keeps only its legitimate job — ranking *which build* a given character uses. Character INCLUSION becomes a diversity guarantee, and the **team-context scorer does the judging.**

**Design.**
1. **Diverse candidate set, not a score-gated one.** Seed the search with the best variant of EVERY owned character (bounded by roster size, ~10–15), so no character is excluded pre-team. (Roster is small, so this is cheap.)
2. **Rank teams by the team scorer.** Score candidate teams with `EstimateTeamDamage(team) + refinements`. This judges amplifiers/supports/synergy in context — correctly.
3. **Bound runtime with a team-context shortlister, not a per-character gate.** All combos × variants is too many, so shortlist — but the shortlist score must itself be **team-context** (e.g. a cheap pyramid-coverage estimate of the 3-character seed), NOT a per-character sum, or it re-introduces the exclusion. Replace/realign `ScoreSupportSeedForAnchor` + `ScoreTeamSkeleton` with a team-context coverage estimate.
4. **Per-character variant ranking (the reverted 2.2 idea) can return LATER, scoped correctly:** use it only to pick a character's own best build (off-hand/sub composition), never to gate inclusion — and ideally make even that team-aware for support builds.

**Suggested first increment (low-risk, measurable):** widen the support-candidate pool to include every owned support character's best variant (full diversity), keep the legacy skeleton scoring only as a *shortlist ordering* (not a hard gate), and let the team scorer rank. Measure runtime (current ~4.5s; the worry is combinatorial growth). If acceptable, that alone fixes the exclusion with zero new per-character heuristics. Then iterate the shortlister toward team-context coverage.

**Start by MAPPING the pipeline** (`BuildTeamSkeletons`, `ScoreSupportSeedForAnchor`, `ScoreTeamSkeleton`, the seed pool ordering, `SkeletonExpansionLimit`) before changing it — the lesson from the 2.2 detour was that patching downstream symptoms without the map causes exactly this kind of handcuffing.

#### Pipeline map (DONE — the named gates a team-dependent support gets filtered at)

Flow: parse inventory → `BuildAdaptiveSearchProfile` → `BuildCharacterSeedVariants` (main-weapon-only seeds) → **`ApplyAdaptiveCharacterShortlist`** → `BuildTeamSkeletons` (anchor select → per-anchor support rank → pair → `ScoreTeamSkeleton` → cap) → expand skeletons to full variants → `BuildTeamCandidatesForCharacters` (trio loop + optimistic-ceiling prune) → `OrderTeamCandidatesForSelection`.

Every character-INCLUSION gate and whether its score is per-character (isolated) or team-context:
- **GATE 1 — `ApplyAdaptiveCharacterShortlist` (PRIMARY).** When roster ≥ `CharacterShortlistLimit` (Adaptive wide=10 / very-wide=8; Default=∞), ranks characters by **`BestVariantSelectionScore`** = best variant's `GetVariantSelectionScore` — **PER-CHARACTER, isolated.** Role minimums (DPS 2 / Healer 1 / Support 1 / Tank 1) + required-effect providers are force-kept, then fill by solo score. A high-synergy / low-solo support outside the minimums is **permanently dropped here, before any team-aware scorer runs.**
- **GATE 2 — anchor selection** (`BuildTeamSkeletons`, `ScoreAnchorCandidate` + `GetVariantSelectionScore`): PER-CHARACTER, `.Take(max(6, SkeletonExpansionLimit))`.
- **GATE 3 — per-anchor support pool** (`ScoreSupportSeedForAnchor`, `.Take(max(6,min(10,limit)))`): **TEAM-CONTEXT-aware** (anchor+support synergy + pyramid coverage) BUT carries `GetVariantSelectionScore(support) × 0.85`, so a low solo score still drags it down. `ShouldPreserveContextualSupportSeedVariant` rescues up to 2.
- GATES 4–6 — per-character variant `.Take` caps (off-hand/ult/sub/retained-variants) all by `GetVariantSelectionScore`; then team-context: skeleton cap, optimistic-ceiling prune, final `OrderTeamCandidatesForSelection`.

**Diagnosis:** a strong-but-team-dependent support is trapped between GATE 1 (shortlist drops it for low solo score) and GATE 3 (support pool drags it via the 0.85 solo term). The team scorer (`ScoreTeamSkeleton`, and ultimately `FinalizeTeamCandidate`/`EstimateTeamDamage`) is correct but never sees it. This is also why 2.2 (which changed `GetVariantSelectionScore`) cascaded — that one function drives GATES 1, 2, 3 (partially), 4–6 and the seed-pool order.

#### First increment (designed, low-risk, validated-bar): make character INCLUSION team-aware
Replace the **per-character** ranking at the two inclusion gates with a **team-aware character value**, leaving build-level `.Take` caps (gates 4–6, which legitimately pick a character's own best builds) alone:
1. **GATE 1 (shortlist):** rank characters by their best *team-pair* value — `max over top anchors of ScoreSupportSeedForAnchor(anchor, char.bestVariant)` (a metric that already exists and is team-aware) — instead of `BestVariantSelectionScore`. So a high-synergy support ranks by its real contribution.
2. **GATE 3 (support pool):** widen to keep every shortlisted character's best support variant (roster is small), and/or drop/shrink the `× 0.85` solo term so `ScoreSupportSeedForAnchor` leans on its already-team-aware synergy/coverage terms.
3. **Let `ScoreTeamSkeleton` + `FinalizeTeamCandidate` do the final judging** (already correct).
HARD VALIDATION BAR (per user — validity over concessions): after the change, on the repro the recommendation must still include the user's primary team AND the genuine alternatives, ranked sensibly, with no off-element/heal-carry-main anomalies and runtime in the same ballpark (~3–5s). Validate against multiple scenarios, not just Aerith.

#### ⛔ CRITICAL CONSTRAINT discovered while attempting the rework — SEEDS ARE MAIN-WEAPON-ONLY

Attempted step 1 (team-aware GATE 1) + step 2 (re-introduce damage-aware `GetVariantSelectionScore`). Step 1 was green but a no-op (legacy scores already include everyone). With step 2 on, **Aerith was STILL excluded** — and the reason is deeper than the gates: **`BuildCharacterSeedVariants` produces MAIN-WEAPON-ONLY seeds (no off-hand, no ULTIMATE, no costume).** So at the seed/skeleton stage where characters are gated, Aerith's seed is just Festive Rod — **Abraxas (her ultimate) is not present to be scored.** Any damage-based seed score (2.2's `EstimateTeamDamage([seed])`, AND the proposed team-aware `EstimateTeamDamage([anchor, support-seed])` fix) **structurally cannot see an ultimate-based amplifier**, so it undervalues amplifier-supports. (Legacy survives because its `BaseScore` + coverage bonuses don't depend on the ultimate.)

Also note: GATE 1 used `ScoreSupportSeedForAnchor`, which carries `GetVariantSelectionScore(support) × 0.85` — so when 2.2 makes that score large, GATE 1 is **damage-polluted**, not genuinely team-aware. Same for GATE 3.

**Reframed rework scope (the real shape).** Making candidate generation damage-aware requires, in order:
1. **Seed-stage scoring must be ULTIMATE-AWARE.** Either (a) compose seeds with each character's best ultimate (and key off-hand) before gating — more upfront cost, or (b) add an "ultimate potential" term to the seed-stage character value that estimates the best ultimate's contribution (esp. amplifiers: credit a debuff/buff amplifier against the team's base). Without this, no damage-aware seed gate can value Aerith.
2. **De-pollute the gate scores:** rank character/support INCLUSION by a true team-context metric (`EstimateTeamDamage` of the pair/seed-with-ultimate), NOT `ScoreSupportSeedForAnchor`'s `×0.85` solo term.
3. THEN the per-character damage variant ranking (2.2) can ride on top without excluding anyone.

**This session's outcome:** reverted BOTH step 2 (damage-aware variant score) AND step 1 (team-aware GATE 1) → back to the EXACT validated, Aerith-inclusive baseline (BEST = Yuffie/Cloud/Aerith, runtime ~3.9s, suite green). GATE 1 was reverted too because it leaned on `ScoreSupportSeedForAnchor` (which the rework must replace with a true team-context, ultimate-aware metric), so it was a half-measure not worth banking. The rework is now correctly scoped around the seed-ultimate constraint — bigger than first thought, and NOT something to chase further under one session's pressure. **Next session starts at constraint #1: ultimate-aware seed-stage scoring**, then de-pollute the gates (team-context `EstimateTeamDamage`, not `×0.85` solo), THEN damage-aware variant ranking on top. KEPT this session: the validated damage model, team-score swap, 2.1 carry orientation, and the additive multi-team groundwork.

### ✅ PHASE 2.2 LANDED (variant selection is damage-aware) — HISTORICAL (reverted, see above)

`GetVariantSelectionScore` now uses, for offensive builds, `offensiveBackbone = EstimateTeamDamage([variant]) + GetVariantTeamBuffContribution(variant)` in place of `variant.BaseScore` (legacy bonuses kept as refinements). Two parts:
1. **`EstimateTeamDamage([variant])`** — ranks a character's builds by damage contribution. For a carry this dominates (own high damage) → off-hand/sub composition chosen for the effects that most raise the build's product. Result on the partial-armory repro: **Cloud's main improved Noble Parasol → Fusion Sword** (his real damage weapon).
2. **`GetVariantTeamBuffContribution`** (support valuation) — a support's value isn't own damage but how much its TEAM-WIDE coverage (All-Allies buffs + enemy debuffs + All-Allies passives) multiplies the CARRY. Valued as `(teamWideMultiplier - 1) × ReferenceCarryWeaponPercent (1100)`. Without this the single-character estimate undervalues a support's All-Allies buffs ~10× (they'd only multiply the support's own low weapon %), which could prune a strong support build before the team comparison.

**Aerith→Cid investigation (resolved):** before 2.2 the team was Yuffie/Cloud/Aerith; after 2.2 it became Yuffie/Cloud/Cid. Hypothesised support-undervaluation, added the team-buff proxy to test it — **the team stayed Cid even with supports correctly valued.** So Cid is a LEGITIMATE damage-model preference (2.2 composed Cid's builds well enough that his team out-estimates Aerith's for this armory; user had flagged Yuffie/_/Cid as plausible). Likely because Aerith's headline value leans on Abraxas, a debuff AMPLIFIER worth ~0 without base debuffs. Both 2.2 changes kept (zero regressions, 233 pass). Pending: user domain judgment on whether Cid is right here, then UI-validate, then Phase 2.6 (AOE).

### ✅ SWAP LANDED (offensive builds now rank by EstimateTeamDamage)

After the 5 misaligned/brittle tests were deleted (Cait-Trumpet + 4 snapshots) the backbone-replace swap was re-applied to both scoring points (`FinalizeTeamCandidate` + `ComputeTeamScoreWithoutSubWeapons`): for `PreferredDamageType != Any`, the team score = `EstimateTeamDamage(baseVariants, request)` + full-weight refinement terms (dedup penalty, requirement bonuses, alignment penalties). Full suite: **233 pass / 1 pre-existing fail / 7 skip** — green. Coverage benchmarks (Kaiser magical-fire, earth physical, etc.) pass; phys-lightning produces Yuffie/Cloud/Aerith with Winged Chakram carry main + Abraxas on Aerith. Suite also much faster (44s vs ~6 min) after dropping the slow exhaustive snapshot tests.

The live offensive core is now the real multiplicative damage estimate: element/axis-gated weapon %, family-coverage product (cross-family compounds, same-family caps), amplifiers (+2 tiers), defense-debuff denominator 1/(1-d), bucketed uptime, passive ability-potency/stat terms, carry cast-share weighting, off-axis flexibility.

**Validation done:** single-enemy partial-armory smoke check → expert-quality team (Yuffie carry Winged Chakram+Rising Sun, Cloud Ragnarok, Aerith Festive Rod+Abraxas), no anti-properties.

**⚠️ AOE smoke check (MultipleEnemies) — confirmed 2.6 gap / partial regression.** Re-ran the same partial armory with `TargetScenario = MultipleEnemies`: the recommended team was **byte-identical** to the single-enemy build. `EstimateTeamDamage` does NOT read `TargetScenario` at all, so replacing the (target-aware) legacy backbone made AOE scoring fall back to single-enemy logic. Not broken (if the inventory's best on-element weapons are single-target, the identical build may be the best *available*), but the model currently CANNOT make the single-vs-AOE tradeoff: it won't prefer AOE/cleave weapons for multi-enemy, nor range-match Damage-Received-Up. **This is the 2.6 work, and landing the swap turned it from "refinement" into a real AOE-awareness gap.** Single-enemy (the default/priority) is unaffected and solid.

**AOE team-STRUCTURE note (user, forward-looking for 2.6):** AOE teams can be similar to the single-target team but with slight structural differences — e.g. **the carry may change** (Cloud could become the main DPS with an AOE/cleave weapon like Erdrick's Sword, since cleave value scales with enemy count). So 2.6 isn't only per-weapon weighting — once `EstimateTeamDamage` is target-aware (AOE weapons weighted by enemy count + range-matched Damage-Received-Up), the carry-selection and team-selection should naturally shift toward AOE-appropriate carries. ORDERING: do Phase 2.2 (variant ranking by damage) FIRST — it's shared infrastructure that flows through `EstimateTeamDamage`, so making `EstimateTeamDamage` target-aware in 2.6 afterward upgrades the variant ranking to AOE-aware for free (no re-work). Validate single-target as a known-good baseline before branching to AOE so the AOE deltas are isolated. Then optional follow-ups: 2.6 range-matched dmg-received-up, 2.7 enemy-resist off-element input, fix the off-axis-flexibility unit test's manual-tier shortcut, address the pre-existing SyntheticPerProviderTeamwideCoverage failure.

**Conclusion / ordering:** the swap is directionally correct but PREMATURE.  <!-- superseded by SWAP LANDED above -->> The damage model must first capture, in `EstimateTeamDamage`: (a) **uptime** (Phase 1d, bucketed), (b) **support-buff value** (already partly there via team-wide All-Allies passives — verify it credits a support's main-slot buff), (c) the **team-wide dedup penalty** (subtract it, or rely on best-per-key + an explicit redundancy penalty), and (d) **range-matched dmg-received-up** (2.6). THEN re-apply the swap. So the real next step is **completing `EstimateTeamDamage` (2.2 + uptime + dedup)**, not the swap. Reverted to keep the suite clean (back to 2.1: 236 pass / 1 pre-existing / 7 skip). The element-gated weapon damage and carry orientation (2.1) stay — they're verified and regression-free.

## Phase 2 calibration answers (user, top player)

1. **(2.7 — IN PROGRESS) Off-element penalty is ENEMY-RESIST-DEPENDENT, not flat.** An off-element weapon loses the weakness-exploit and can't use the element resist-down / element-damage-up terms (a fixed loss) AND may be resisted by a variable amount. So `GetWeaponEffectiveDamagePercent`'s 0.5 off-element factor should become a **per-fight enemy-resist input** with a moderate default (~0.5) when unknown — not a hard constant. (On-element and off-axis factors stay as-is for now.)
2. ✅ **(2.6 — DONE) Single-target vs AOE is about RANGE-MATCHED Damage-Received-Up, not raw damage.** LANDED (carry-range approximation): `WeaponSearchItem.Range` plumbed → `PlayerPowerAnalyzerV2ItemSlot.Range`; `EstimateTeamDamage(baseVariants)` now drops **single-enemy** `damage_received_up` debuffs when the **carry** swings an all-enemies weapon (they wouldn't apply to its hits); all-enemies dmg-rcvd-up always applies; unknown range defaults to single-target (never loses coverage). Uses the already-parsed `DetectedActiveEffect.TargetScope`. Suite green; only changes teams with a single-enemy dmg-rcvd-up + AOE carry. Full PER-ATTACKER matching (vs carry-approximation) is a later refinement. AOE weapons can hit very hard — Erdrick's Sword (all-enemies range) still gets its **1.5 multiplier**, plus resist-down, PDEF-down, etc. The ONE thing it loses: a **single-target** Damage-Received-Up does NOT apply to an all-enemies-range attack; only an **All-Target Phys. Dmg. Rcvd. Up** (a separate buff) does. Implications for the model:
   - Each weapon/ability needs a **range** attribute (single-target vs all-enemies). The 1.5-style multiplier is **ability-specific** and already in the weapon %, NOT a single-target-only bonus — do not blanket-favor single-target on damage.
   - Split the `damage_received_up` family by target scope: a **single-target** dmg-rcvd-up only multiplies a single-target attacker's damage; an **all-target** dmg-rcvd-up multiplies any attacker. Match per-attacker by that attacker's weapon range.
   - **Range only matters for DPS.** For Support/Tank/Healer providing buffs/debuffs/effects, single-target vs AOE is irrelevant in a single-enemy fight (their effects apply regardless).
3. **Team shape varies by armory/fight** — no fixed carry/support split. The cast-share weighting (sort attackers by weapon %, assign 0.6/0.3/0.1) already self-adapts: supports have low weapon %, so they naturally fall to low shares; a 2-DPS team naturally splits its shares between two real attackers. Keep it adaptive, don't hard-code a shape.
4. **Support variant ranking = broad pool + coverage-breadth tiebreak.** Don't over-prune support characters' builds per-character; keep a wide diverse pool AND rank within it by how many distinct missing pyramid layers the build covers (at good %s), so the team-level damage estimate has good options to choose from once the carry is known.

## 🔬 Selection-foundation AUDIT (done — verdict: ~1/3 structural KEEP, ~2/3 legacy heuristic REPLACE/REDESIGN)

Walked every scoring/gating function in the selection path and classified each Keep / Replace-with-damage-model / Redesign. **Headline:** `EstimateTeamDamage` (the trusted multiplicative model) runs ONLY at the final `FinalizeTeamCandidate` stage; everything upstream that GATES which characters/seeds/skeletons survive runs on hand-tuned legacy heuristics — and on **main-weapon-only seeds** (no ultimate/outfit). "The damage model is sitting at the bottom of a funnel whose walls are built from the thing it was meant to replace."

**KEEP (structural / real mechanics the model consumes as INPUT, not proxies):** `BuildAdaptiveSearchProfile` (search-budget caps), mutual-exclusion + role-template (×0.5) + role-quota filters, `PreferredCoverageBonus` + hard/soft requirement signals, `GetVariantWeaponOrientationSelectionBonus` (R-ability half-value-off-hand rule — see [[r-ability-slot-orientation]]), active-buff shared-cap **dedup** mechanics (redundant-off-hand + team-wide duplicate penalties — see [[duplicate-buff-dedup]]).

**REPLACE (hand-tuned damage proxies that gate/rank — should be driven by `EstimateTeamDamage`):** `GetVariantSelectionScore` (the master per-character proxy: DPS base 240, anchor element +132, main-stat ×0.18, DamagePercent ×0.12, tier combos +115/135, off-axis −170/240…) + its bonus helpers (`GetAnchorCandidateSelectionBonus`, `GetVariantRequestedAxisMultiplierSelectionBonus`, `GetVariantRequestedElementCarrySelectionBonus`, `GetVariantOffensiveRoleSelectionBonus`, `ScorePyramidCoverage` +120/82/68/44); `ScoreAnchorCandidate`; `ScoreSupportSeedForAnchor` (×0.85 solo); `ScoreTeamSkeleton` (a full PARALLEL team-damage estimator — pyramid +120/82/68/44, hard ×70 / soft ×45, offensiveShell ×0.45); `BaseScore`'s additive ranking use; refinement `variantAlignmentPenalty` + `offensiveShellScore` + the axis half of `contextualVariantBonus`.

**REDESIGN (structurally conflict with team-context damage scoring):** `BuildCharacterSeedVariants` (main-only seeds hide ultimates/outfits at the gating stage), `ApplyAdaptiveCharacterShortlist` + `BuildTeamSkeletons` (gate team composition on ultimate-blind seeds via the proxies above, before the damage model runs).

**Top-3 highest-leverage targets (in dependency order):**
1. ✅ **DONE — REDESIGN the ultimate-blind funnel** (`BuildCharacterSeedVariants`). Seeds now compose each main with the character's BEST off-hand + ultimate + outfit (was main-only, null off/ult/outfit), with the ultimate/costume dictionaries + costume cache wired through the caller. The gating stage now sees a character's ultimate (always-on stats + R-ability passives, and amplifier presence). Only the MAIN carries into the downstream expansion (which re-explores off/ult/outfit on surviving seeds), so this sharpens GATING accuracy without locking final builds. Validated: primary team preserved (Yuffie/Cloud/Aerith, Aerith included), runtime ~3.9s, suite green (note: total tests dropped — removed the brittle reflection diagnostic `AerithWeaponDiagnostics`; a process slip also removed one adjacent DISABLED `[Fact(Skip)]` exact-shell sub-weapon snapshot, recoverable from transcript). Side effect for the user's eye: off-hand picks shifted (Yuffie Bird of Prey→Wind Slash, Aerith Rousing Wizer→Kamura Wand) — a downstream ripple, team intact.
2. ✅ **DONE — REPLACE `ScoreTeamSkeleton`.** For damage-typed requests it now ranks skeletons by `EstimateTeamDamage(seedVariants)` instead of the legacy parallel proxy (Σ GetVariantSelectionScore + effect-package + pyramid-coverage + offensive-shell + synergy); kept the structural refinements (PreferredCoverageBonus + hard×70/soft×45 + template-role ×0.7), and kept the full legacy heuristic for the `Any` (no-axis) case. The skeleton GATE and `FinalizeTeamCandidate` now agree. Validated: primary team preserved (Yuffie/Cloud/Aerith), **runtime IMPROVED to ~3.1s** (EstimateTeamDamage ≈ cheaper than the heuristic bundle it replaced — no two-stage prefilter needed), suite green, and **Cloud's main improved Noble Parasol→Fusion Sword for free** (the real model surfaced the better Cloud seed to survive expansion — with NO change to the variant score yet).
3. ⏸️ **ATTEMPTED + REVERTED — REPLACE `GetVariantSelectionScore` + `ScoreAnchorCandidate`.** On the #1+#2 foundation, swapping the per-character backbone to `EstimateTeamDamage([variant])` (and adding the reference-carry support term, and support-pool character-diversity) **still excluded Aerith** (BEST→Yuffie/Cloud/Cid). ROOT CAUSE (now proven structural): **no PER-CHARACTER score — legacy or damage-aware — can rank a PURE support (Aerith, buffs only) vs an ATTACKER-support (Cid, damage + buffs); that is a 3-character TEAM decision.** And multiple GATES cut on the per-character score (the adaptive **shortlist** `BestVariantSelectionScore` on wide rosters, the **support pool** `ScoreSupportSeedForAnchor×0.85`, and the **skeleton-expansion cut** where `EstimateTeamDamage` itself may rank Cid-teams ≥ Aerith-teams). So the exclusion cascades through ALL of them; fixing one doesn't help. Reverted to the #1+#2 state (Aerith primary + Cid as a genuine ALTERNATE + Cloud main = Fusion Sword, ~3s, suite green). **A real #3 = full multi-gate de-pollution** (shortlist + support pool keep diversity, let `EstimateTeamDamage` decide the 3-char team) — a larger deliberate effort whose payoff is uncertain because the model may legitimately rank Cid ~even with Aerith (the user's "close call, Aerith easier to pilot"). Given #1+#2 already produce the wanted team AND surface Cid as an alternate, #3 is **deferred unless the user wants it.** The "lean-replace" sub-bonus cleanup below is moot until then.

**"Lean-replace" items to verify against the damage model's coverage first:** `GetVariantLowActualUsePenalty` (buff that rarely FIRES — the cast-economy/uptime nuance the model may not fully capture), `GetVariantGearCAbilityUsesSelectionBonus` (limited-use ability counts), `referencePatternBonus` (meta-archetype knowledge the model lacks — and which can FIGHT damage ordering). Keep only if they encode something `EstimateTeamDamage` genuinely doesn't.

**RUNTIME CAVEAT for the rework:** the legacy heuristics are cheap arithmetic; `EstimateTeamDamage` is pricier (effect aggregation + product). Replacing per-variant/per-skeleton gating scores with full `EstimateTeamDamage` calls (which run MANY times in the search) risks runtime regression (~3–5s budget). Likely need a cheap team-context approximation at the wide seed/skeleton stage + full `EstimateTeamDamage` on the narrowed shortlist — i.e. keep the funnel STRUCTURE, swap the scoring, mind the call count.

### Mechanics clarifications (pre-rework) → design implications

From the user's pre-rework mechanics walkthrough (full detail in memory `battle-flow-atb-and-conditional-effects`):
- **Ultimate-aware seeds (target #1) — the rule is now precise:** an ultimate contributes **base stats + passive R-abilities ALWAYS**, plus its **active ability discounted by charge/usage**. So seed valuation must count the ultimate's stats+passives unconditionally (this alone fixes the old main-only-seed blindness for amplifiers like Abraxas) and the active with a charge factor. Off-hand actives matter too → seeds should likely carry best-ultimate AND best-off-hand, not just main.
- **Cast economy is REAL but second-order:** ATB is scarce early (start 2; ~4/10s, 6 with Haste), you can queue only one ability ahead, so "all buffs up turn-1" is optimistic and many-application teams ramp slower. BUT it's battle-length-dependent and hard to model precisely → keep the **bucketed-uptime approximation; do NOT build a cast-economy simulator.** Overspeed (1-less-ATB) and branding/materia stats → **hold constant** for ranking.
- **`GetVariantLowActualUsePenalty` (lean-keep) is justified** — "a buff that barely fires" is a genuine effect of ATB scarcity; keep a light version.
- **Conditional effects the rework should make TEAM-CONTEXT or SKEPTICAL, not flat bonuses:**
  - **ATB Conservation** → usually a net loss (short ~10s duration vs 4-ATB cost). **Deprioritize/down-weight** (legacy likely over-rewards it).
  - **Limited-use / Gear-C extra uses** (e.g. Garb of the Possessed) → value is **battle-length-dependent**; we can't know length at build time → keep mild/conditional, not a strong signal.
  - **HP Gain** → only valuable when the team runs **healer-less**; make it conditional on team composition, not a flat reward.
- **Validation discipline:** the user detects "sounds good but there's a better option" by SEEING recommended teams → validate each rework step against real recommendations, iteratively.

## Future phase (user-proposed) — multi-team output ("here are your better options")

Instead of exposing a single "best" team, surface the top few **distinct, strong** team options and let the player decide by testing in-game. Strongly motivated: (a) close calls are common and **pilot-dependent** (the Cid-vs-Aerith analysis below is so close it "comes down to the player"); (b) it makes the established "no single correct team" validation philosophy visible to the user; (c) **it is ADDITIVE, not a scoring change** — it exposes more of what the engine already ranks internally, so it can't degrade the quality of the underlying suggestions (the user's explicit concern about risky sweeping changes).

**Design:**
- **Service (moderate, low-risk):** retain the top-N team candidates instead of just the argmax. Apply a **diversity filter** — the raw top-N are near-duplicates (same roster, minor sub-weapon swaps), so group by a meaningful key (distinct character roster, or distinct carry/support strategy) and surface the best of each group → "top-N DISTINCT options," not "top-N teams."
- **Per-option rationale (high value-add):** a short "why this option / what's the tradeoff" per team — the same differentiating-pyramid-layer comparison done by hand for Cid-vs-Aerith (e.g. "more coverage + own damage but harder to maintain PATK" vs "reliable buffs + Abraxas amplification, easier to pilot"). The engine has the data to generate this.
- **UI (the bigger lift):** display multiple teams, expand/compare, show each rationale.

**Sequencing:** do this AFTER the single-best ranking is trustworthy (multi-team surfaces the top-N of the same ranking; if the ranking is good the top-N are all good). So: validate single-target → (optionally AOE / 2.6) → then multi-team output. Pairs with the **cast-economy limitation** (deferred): the model takes buffs at face value and can't see the opportunity cost of maintaining a fragile buff stack (e.g. Cid casting Bravery to hold PATK competes with Antler Pike) — a real but deep refinement; multi-team output sidesteps the need to perfectly resolve such close calls by handing them to the player.

## Validation philosophy (user) — property/ranking, NOT exact shells

There is **no single correct team** — multiple are valid and the best depends on the player's (often partial) armory. For phys-lightning the user named several plausible teams: Yuffie/Cloud/Aerith (their best — Abraxas debuff-amp + sub-DPS + full coverage), OSeph/Zack/Yuffie (slightly light on lightning resist), Yuffie/OSeph/Cid (Bravery materia to hold PATK Up after Ragnarok). So the analyzer's job is to land in the SET of good teams and avoid egregiously-wrong ones — not to reproduce one shell.

Therefore Phase 2 validation should be **property-based and ranking-based**, not exact-shell snapshots (which is exactly why the snapshot tests kept needing re-baselining — they over-pin a legitimately multi-valued output):
- **Properties of a good offensive team:** carry's main is an on-element/on-axis high-% weapon; key pyramid layers covered (PATK Up, Exploit Weakness, element resist down, element damage up, ideally damage bonus/boost + enliven/enfeeble); amplifiers (Abraxas/Enliven) paired with a base buff/debuff to amplify.
- **Anti-properties (must NOT happen):** off-element/off-axis weapon as the carry main; heal/sustain weapon as a DPS main; redundant same-layer stacking; situational (sigil/interrupt) effects counted as persistent.
- **Ranking checks:** the walkthrough team and its honorable mentions all score near the top; anomalous builds score clearly lower.
- Prefer replacing the brittle exact-shell snapshot tests with these property/ranking assertions during the Phase 2 re-baseline.

## Pre-UI-validation polish (LANDED)
- ✅ **2.6 range/AOE** — `Range` plumbed to the slot; single-enemy `damage_received_up` no longer over-credits an AOE carry (carry-range approximation; per-attacker is a later refinement).
- ✅ **2.7 off-element** — flat `0.5` off-element factor → per-fight `request.OffElementDamageFactor` (default 0.5; backward-compatible).
- ✅ **Multi-team rationale damage-filter** — `AddsVsBest`/`DropsVsBest` filtered by `IsDamageRelevantEffectKey(key, damageType)` so the alternates' "why" shows only damage-relevant, on-axis trade-offs (drops defensive/sustain + off-axis noise).
- ✅ **Conditional effects** — ATB Conservation trimmed to free-rider-neutral targeting (1.0–1.05, never penalized); limited-use Gear-C bonus was already conditional; **HP Gain is NOT a distinct modeled effect** (only generic HP Recovery → `healing_support`) so its healer-less conditional needs NEW detection — deferred unless wanted.
- ✅ **Test hygiene** — 4 unit tests added (2.7 off-element factor; 2.6 `IsAllEnemiesRange` + `IsDamageReceivedUpFamily`; rationale `IsDamageRelevantEffectKey`); the pre-existing `SyntheticPerProviderTeamwideCoverage` test RETIRED (Skip + reason — it asserted per-provider stacking, contradicting the shared-cap dedup model). **Suite now FULLY GREEN: 0 failures / 236 passed / 7 skipped.**
- **STATUS: item-3 model-completeness + polish COMPLETE; ready for UI validation. Only deferred optional item = HP-Gain effect detection.**

## 🐞 Self-scope buff over-credit (FOUND via UI validation — TOP model-completeness item)
Surfaced by the user's eye on a real Exhaustive run (Cloud / Fusion Sword main): Fusion Sword's buffs are all **Self**-scope, yet Cloud has no Physical-Lightning weapon, so they'd be barely used. The "why" check on the DAMAGE MODEL (not the legacy scorer) confirmed a real, LIVE bug:
- `GetDetectedEffectsForVariant` (ActiveEffects.cs:652) pulls every effect off a character's slots with **NO scope filter** — Self-scope buffs included.
- `BuildActiveFamilyState` (DamageModel.cs:142) pools them into ONE team-wide multiplier — checks family + axis but **never `TargetScope`** — so a Self buff is treated exactly like an All-Allies buff.
- The core applies that single multiplier to the **cast-share-weighted sum of ALL attackers**. So a 2nd-attacker's (Cloud's) Self buff is credited as if it multiplies the whole team's damage — dominated by Yuffie's big on-element hits — when it should only boost Cloud's own 0.3-share, off-element (×0.5) portion. **Over-credit ≈ the entire rest of the team's damage.**

**Why it matters (two ways):** (1) it's a LIVE accuracy bug in target #2's team selection TODAY — self-buff-heavy teams are over-scored team-wide; (2) it means **de-pollution alone would NOT fix the Fusion Sword class of problem** — `EstimateTeamDamage` (the thing de-pollution defers to) shares the blind spot and would reproduce it. So this is a hard prerequisite for de-pollution AND an immediate quality win.

**FIX SCOPE — "Self-scope buff confinement" → really SCOPE-AWARE per-attacker buff application.** The fix is broader than just Self; the ally-buff scope hierarchy each needs the right target:
- **Self** → the OWNER's share only.
- **Single Ally** (`[Rng.: Single Ally]` — player CHOOSES one ally at cast, like Single Enemy; for damage they pick the optimal target) → the **carry / best attacker's** share, NOT team-wide and NOT the owner.
- **All Allies** → every attacker (shared multiplier — current behavior, correct).
- **Other Allies** → every attacker EXCEPT the caster.
- **Single/All Enemy** debuffs → per attacker by range-match (the 2.6 work — pairs here).

Requires PER-ATTACKER damage application (same restructure as deferred 2.6): compute the shared (All-Allies + matched enemy-debuff) multiplier, then for each attacker multiply their weighted weapon% by (shared × their OWN Self-buffs × any Single-Ally buffs assigned to them as the best target × Other-Allies buffs from teammates). Scope + owner are recoverable: effects carry `TargetScope` (Self/SingleAlly/OtherAllies/AllAllies/SingleEnemy/AllEnemies) + `SourceName` (owning slot). NOTE: verify `[Rng.: Single Ally]` parses to `SingleAlly` (wasn't in the 6-weapon sample) before relying on it.

**✅ LANDED.** `EstimateDamageMultiplier` extracted (multiplier for one effect set); `EstimateTeamDamage(baseVariants)` partitions effects by `TargetScope` (Self→owner, Single-Ally→carry, Other-Allies→teammates, rest→shared) and sums per-attacker `weapon% × share × multiplier(shared + that attacker's own/assigned effects)`. **RESULT (correct + honest):** removed the self-buff inflation → Aerith-vs-Cid went from a wide gap (439k vs 385k) to a **near-tie** — Adaptive: **Cid 254k / Aerith 250k** (~1.7% behind, a close visible alternate); Exhaustive: **Aerith 259k / Cid 243k**. So which is #1 now depends on search breadth (they're genuinely ~equal — the user's "close call, Aerith easier to pilot"), and multi-team shows both. Suite green (236 pass); DamageModel unit tests unaffected (uniform-multiplier core kept).

**RUNTIME — cheap-gate / precise-final (with a hard lesson).** Per-attacker ≈ 2× the multiplier work. Naive (per-attacker everywhere) = 31s Exhaustive. **The pruning ceiling (`ComputeTeamScoreWithoutSubWeapons`) MUST stay precise** — making it uniform/over-credited = too loose → pruning stops → 417s (7 min). So only the high-volume **skeleton GATE** (`ScoreTeamSkeleton`) uses `scopeAware:false` (uniform, safe: it only over-credits, never under-ranks). Net: **Adaptive (common path) ~2.5s — fast; Exhaustive ~29s — slow but it's the completeness mode.** Deferred optimization if Exhaustive speed matters: cache the shared family STATE and add only per-owner deltas (watch the amplifier cross-term). **PRIORITY: top of model-completeness, ahead of de-pollution.**

**PREREQUISITE — verify effect PARSING before relying on it (user-flagged, possibly under-researched):** the confinement fix (and the whole damage model) is only as correct as the effect-DETECTION layer that turns weapon/costume ability TEXT into `{key, familyKey, axisKey, TargetScope, tier→%, explicit %}` (`TryAddDetectedEffect` / `ParseTargetScope` / the tier-and-% resolution in ActiveEffects.cs). The detection is pattern/string-matching on ability text, so **wording variants can cause an effect to be mis-keyed, mis-SCOPED (Self vs All-Allies vs Single/All-Enemy — the exact thing confinement hinges on), mis-tiered, or missed entirely** — and then it's silently applied/assumed wrong everywhere downstream. Before/with the confinement fix, do a VERIFICATION pass: enumerate every detected effect across the full owned-weapon/costume corpus, and (a) flag ability texts that parse to NO effect (possible missed coverage), (b) flag effects whose `TargetScope == Unknown` or whose tier/% didn't resolve, (c) spot-check a sample of Self vs All-Allies vs enemy-debuff parses against the real labels. Treat any mismatch as a detection bug to fix, since confining/aggregating on a wrong scope is worse than the original over-credit. (This is plausibly an under-tested area — worth its own focused audit, not just incidental checks.)

**PARSING AUDIT — FIRST FINDINGS (6 sample weapons; diagnostic run + removed).** Detection runs on `AbilityText` prose, which carries precise `[Rng.: ...]` scope + `[Pot: ...]` markers per effect.
- ✅ **GOOD — `TargetScope` parses RIGHT:** Self / All-Allies / All-Enemies / Single-Enemy all correct from `[Rng.:]` (Fusion Sword weapon-boosts = Self ✓, Ragnarok amps = Self ✓, Noble Parasol dmg-rcvd-up = All-Enemies ✓, Winged Chakram PDEF Down = Single-Enemy ✓). **So the Self-scope confinement fix is SAFE to build on the `Self` tag** — the audit does NOT block it.
- ✅ **GOOD — damage-relevant magnitudes parsed correctly** in the sample (weapon-boost 40%, damage-bonus 30%, dmg-rcvd-up 50%, PDEF-Down / Elem-Dmg-Up / tier-increase = Mid→tier 2, amps 40%).
- ❌ **GAP 1 — RETRACTED (was "ENLIVEN missed on Noble Parasol").** The CORPUS-WIDE cross-check below proved this is NOT a detection bug: Noble Parasol's prose has NO Enliven at all (it has Dmg-Rcvd-Up + Haste); the `EffectTags` metadata falsely lists Enliven. The PROSE DETECTION was right; the `EffectTags` is the unreliable source.
- ⚠️ **GAP 2 — 2.6 range-matching uses the WRONG signal:** "Single-Tgt." vs "All-Tgt." Dmg-Rcvd-Up (which ATTACK TYPES benefit) is in the effect NAME and is DISCARDED (both → key `phys/mag_damage_received_up`); the parsed `TargetScope` captures `[Rng.: All Enemies]` (which ENEMIES get the debuff), NOT the attack-type. So 2.6's filter (drop `TargetScope==SingleEnemy` for AOE carry) keys off the wrong thing. **2.6 needs the Single/All-Tgt name distinction preserved (separate keys or a flag), then matched to the attacker's weapon range.**
- ⚠️ **GAP 3 — limited-use / conditional treated as persistent:** Ragnarok "Amp. Abilities [Count: 1][up to 1 time(s)]" parsed as a persistent +40% (should be one-shot); Winged Chakram "When a debuff is on the target, Lightning Damage Up" parsed as always-on (conditional ignored). Over-credits these.
- ⚠️ **GAP 4 (low for damage) — tier/% mis-association:** non-damage effects grabbed a neighbor's `[Pot:]` (healing_support got 30% from the damage bonus; Haste got tier 2 from the stat-debuff "Mid"). Damage-relevant effects were correct in the sample, but the association is fragile when multiple effects share text.
- **`EffectTags` is NOT authoritative** (Abraxas's tags = only [Haste], MISSING the stat-debuff amplifier that AbilityText detection DID catch) AND feeding tags into the blob corrupts scope. Best use of `EffectTags`: a **cross-check guard** — flag effects present in `EffectTags` but NOT detected from AbilityText (would have caught the Enliven miss).

**TRIAGE / SEQUENCE:** parsing is better than feared for what matters most (scope + damage magnitudes), so the **Self-scope confinement fix can proceed**. Separately, fix the real parsing gaps by priority: 2.6 Single/All-Tgt distinction, limited-use/conditional over-credit, tier/% association robustness.

**CORPUS-WIDE CROSS-CHECK + the CUSTOMIZATIONS correction (user-supplied — my cross-check was INVALID).** I compared each weapon's `EffectTags` vs keys detected from its base `AbilityText`; 28/685 (4%) differed, and I WRONGLY concluded "EffectTags are errors / detection reliable." **The real cause: CUSTOMIZATIONS.** Weapons can have 0–2 customization options (one active at a time) that change the kit — **Spade** adds an effect (Dragoon Lance Spade → **All-Allies Exploit Weakness**; Bird of Prey Spade → Phys. Weapon Boost), **Diamond** reverts the ability but adds a 3rd passive R-ability, **Heart** boosts the ability's **damage %** (Starsoul Blade 1340%→1680%). `EffectTags` reflect the customization-inclusive set; the base prose doesn't. My cross-check ran detection on the BASE text, **bypassing** the customization the analyzer actually applies — so it was testing the wrong thing. The base-text detection IS reliable (6-sample scope/% correct); `EffectTags` is NOT "wrong" — it includes customization effects.

**The analyzer ALREADY handles customizations** (`SelectBestCustomization`, PlayerPowerAnalyzerV2Service.cs:6813): per weapon it scores each customization = `passiveScore + effectScore` (the customization's passive via `ScorePassiveSkill` + its active effects detected from `customization.Description` via `GetCustomizationEffectDelta`), picks the best, applies its passive + adds its effects to the build. So customizations are NOT ignored. **But two REAL gaps remain:**
1. **Selection uses LEGACY per-character heuristic, not the damage model / team-context.** A team-wide customization effect (Dragoon Lance Spade's **All-Allies** Exploit Weakness) is valued for the holder, not by its boost to the team's carry — so the "best customization" choice can be sub-optimal (same class of issue as the per-character vs team-context theme throughout).
2. **The HEART customization (pure damage-% boost) is essentially UNMODELED.** Its `Description` (e.g. "Damage Potency +340%") yields NO detectable buff/debuff → `effectScore = 0` → it scores 0 in `SelectBestCustomization` (rarely chosen) AND the weapon's `DamagePercent` is never increased by it. So a large real damage gain (+340%!) is invisible. (Confirm DamagePercent isn't adjusted elsewhere.)

**So the customization work (the user's open "how to choose the better customization" question):** (a) make customization selection **damage-model-aware / team-context** (evaluate each option's effect on team `EstimateTeamDamage`, including Heart's % as a `DamagePercent` delta, and pick the max); (b) **apply Heart's damage-% boost** to the weapon's effective damage. Note: customizations + passives + effects are OB-level / character-level dependent (weapons service exposes per-snapshot data). This is a real, impactful feature — replaces the legacy heuristic selection with the trusted model.

**✅ VERIFIED (code + data). Customization taxonomy (corpus):** `Diamond` ×59 (`Kind='R Ability'` — adds a passive R-ability; handled), `Spade` ×40 (`Kind='Damage Upgrade'` — `Description` = the FULL modified ability text; its added effects DO reach the model, detected+added; base effects deduped), `Heart` ×6 (`Kind='Damage Upgrade'` — `Description="Damage Potency +340% (new 1680%)"`). Confirmed: **Starsoul Blade base DamagePercent=1340 (NOT the Heart 1680) → Heart not pre-applied; slot.DamagePercent = base snapshot; `SelectBestCustomization` scores only passive+effect → a Heart scores 0 → never chosen AND its % never reaches damage.** So **Heart (6 weapons) is fully unmodeled** — the clean, contained fix. The Spade gap is only the team-context SELECTION (legacy per-character heuristic), which is the same theme as de-pollution. FIX SCOPE: (1) parse Heart's `+340%` / "new 1680%" from the Description → apply as a `DamagePercent`/effective-damage delta; (2) score that damage in `SelectBestCustomization` so Heart can win; (3) [larger] team-context customization selection (carry→Heart, support→Spade) folds into the de-pollution rework. (Minor: Spade's full-text Description double-detects base effects vs the base AbilityText path — deduped by the damage model, but may double-count in legacy selection scoring; verify.)

**✅ HEART FIX LANDED.** `TryParseHeartDamagePercent` (regex `\(new X%\)` primary, `Damage Potency +X%` delta fallback; Spade/Diamond text doesn't match). `SelectBestCustomization` now adds `damageScore = baseDamageScore × (newPct−basePct)/basePct` so Heart can win — **gated on `includeDamage`** (the slot's ability damage must actually count). The slot applies `effectiveDamagePercent` (model reads `slot.DamagePercent`) and scales the legacy damage score by `heartDamageBoostFactor`. Per-character magnitude naturally favors Heart on high-damage carries; team-context selection still deferred. **REGRESSION caught + fixed during validation:** the first cut scored Heart unconditionally → for SUB-weapons (damage not counted) it stole the customization pick from a Diamond passive → broke 2 sub-weapon marginal-gain tests; the `includeDamage` gate fixed it. Unit test `Customization_HeartDamagePercent_*` covers parse (absolute + delta + non-Heart reject). Suite green.

**✅ CROSS-CHECK CONFUSION — DEFINITIVELY RESOLVED (traced the detection path).** The damage-model detection path `GetDetectedEffectsForVariant → AddDetectedEffectsFromSlot` (ActiveEffects.cs:689,701) passes **`Array.Empty<string>()` for tags** and reads `slot.AbilityText` (= `weapon.Snapshot.AbilityText`, BASE text) PLUS `slot.SelectedCustomization` (the chosen customization's Description). So the layer that picks teams **never reads raw `EffectTags`** → the EffectTags-vs-base-prose discrepancies are IRRELEVANT to team selection: stale tags (Noble Parasol's phantom "enliven") are ignored, and real customization effects (Dragoon Lance Spade exploit-weakness) come through the SELECTED-customization path only. The whole "confusion" was my methodology — I cross-checked against `EffectTags`, a source the authoritative layer doesn't use. MINOR honest caveat: the LEGACY per-slot score (`CreateWeaponSlot`:4319) DOES fold raw `EffectTags` into detection, so it can over-credit stale/customization-inclusive tags in the variant-selection proxy (`BaseScore`) — but the damage model re-detects cleanly and OVERRIDES it for team selection, so recommendations aren't corrupted. Optional cleanup: pass empty tags at 4319 for consistency (low value, some test risk). NET: the recommendations are based on clean base-text + selected-customization detection.

**✅ UI TEAM CHOOSER LANDED** (PlayerPowerAnalyzerV2.cshtml). A chooser button bar above the build-out lists every team (★ Recommended default-selected + each Alternate, roster + score); clicking toggles to show THAT team's full build-out. One toggle-able `<div class="team-buildout">` panel per team, reusing the existing per-character markup (outer `for` over `BuildChooserTeams(...)`); each alternate's Adds/Drops tradeoff shown inline; client-side JS toggle (data already in `AlternateTeam.CharacterBuilds`, no model/server changes); redundant "Other Teams" card removed. Compiles + suite green. NOT yet browser-visually-verified. **RAZOR GOTCHAS (cost several build iterations — remember):** (1) **named tuples don't parse** in `@functions`/`@{}` (`List<(string Label, ...)>` → RZ1010) — use a small class (`ChooserTeam`); (2) the page's results `@if (...HasResult) { }` body is a **CODE-block context** at the top level, so sibling `var`/`if`/`for` there must be **plain C# (no `@`)**; `@` is only for code nested INSIDE the markup `<div>` blocks.

**✅ OMNI ABILITY-DMG CURVE FIX (costume passive under-credit) — step 1 of the costume stacking work.** The all-6-element "Fire Ice Lightning Earth Water Wind Ability Dmg" omni costume passive (Kisaragi Family Blessing / Savior Goggles family) routed through the generic `ElementPotBonuses` table → 10pt credited as 15% when the real 2-level curve is Lv.1(1pt)=15%, Lv.2(10pt)=30%. MEASURED via temp probe: omni @10pt=15% (should 30%), @5pt(sub)=15% (correct); meanwhile Shinobi's self "Boost Phys. Ability Pot." @15pt=30% (double each costume passive), which is why the selfish costume kept stealing the main slot. FIX: `IsOmniElementAbilityDmgLabel` (all 6 elements + "ability dmg") + `OmniElementAbilityDmgBonuses [15,30]@[1,10]pt`, as the FIRST branch in `TryGetPassiveBonusValue` (fixes both the damage model AND the slot score). Suite green (237), NO snapshot shifts (parity, not a flip — the costume flip needs step 2 stacking + step 3 gate). GENERAL fix (any omni-costume build). **REMAINING (user-approved B+(c), measured steps):** step 2 = per-R-ability damage-model stacking (different-NAMED R-abilities same buff label resolve separately + ADD — Kisaragi 30% main + Savior 15% sub = 45% — not pool; via `{label→{SkillName→points}}` contributions feeding the damage model, leaving the ~100 label consumers + `{label→points}` map untouched). step 3 = model-drive the MAIN costume gate (`BuildCharacterVariants` ~line 583; `teamContextSeeds` already in scope) so a team-superior stack can't be silently `Take(N)`-dropped before the now-correct damage model sees it. RATIONALE: tool suggests specific build-outs to novices who use them verbatim → silent slip (as armory changes) unacceptable → robust path over fragile contained one. Spikes: full SkillId re-key = 100+ consumers (NO); contained B = ~5 fns; (c) = 1 fn (MAIN gate only, not the SUB gate inside the loop), modest runtime.

**✅ ALL-ALLIES R-ABILITY STACKING FIX (user-confirmed core scoring bug).** The damage model POOLED same-name team-wide (All-Allies) passive POINTS across providers (`carryEffectivePassives` summed Cloud 46 + Cid 46 = 92pt) then applied ONE breakpoint. But user confirmed: same-name All-Allies R-abilities from DIFFERENT providers **STACK as separate buffs** — Cloud 46pt (`Boost PATK All-Allies` breakpoint table `[1,5,15,25,35,45,55]pt → [5,10,14,18,22,25,28]%` → 25%) AND Cid 46pt (25%) = **+50%** team-wide, NOT pooled 92pt → 28% (the cap). The pooling badly under-credited SPREADING a buff across bodies (the curve flattens hard). FIX (`EstimateTeamDamage(baseVariants)` passive block, DamageModel.cs ~596): resolve EACH provider's team-wide passives to a % at THAT provider's OWN breakpoint via `BuildPassiveFamilyTerms`, then SUM the %s (per-provider stacking) — never sum the points. Carry's own per-character passives unchanged (the carry-centric per-attacker-passive limitation stays — deferred, user-deprioritized). **MEASURED (repro, Adaptive):** BEST Yuffie/Cloud/Cid **247,999 → 252,691** (roster + builds STABLE, Cloud still Erdrick's[Lightning]); the Aerith team rose MORE **189,392 → 209,662** (a support team spreads All-Allies across bodies → was the most under-valued by pooling). Runtime ~2.3s. Suite green (237), no test regressions. NOTE: the LEGACY sub-weapon heuristic still pools via `AggregateTeamWidePassivePoints` (sub-weapons deprioritized — not fixed).

**✅ DE-POLLUTION ROUND 1 (user-committed) — Stage 1 + precise skeleton gate.** Map: team SELECTION (skeleton 1085, ceiling 3628, final 3847) already used `EstimateTeamDamage`; pollution was in build COMPOSITION (per-character variant retention) + coverage bonuses. **Stage 1:** `BuildCharacterVariants` retention now ranks by `EstimateTeamDamage([variant, the two real skeleton teammates], scopeAware:false)` instead of per-character `GetVariantSelectionScore` (`teamContextSeeds` threaded from `BuildTeamCandidatesFromSkeletons`; tiebreak per-char score; falls back to legacy for seed-building / "Any"). Correct + foundational, runtime FINE — but had **NO visible impact alone** on the repro, because the win was blocked upstream. **Diagnosis (confirmed):** the Aerith skeleton carried Cloud's **Fusion Sword [NonElem]** seed; with the element-gating fix, Garb/All-Allies effects are worthless to that off-element Cloud → Stage 1 correctly can't pick them. The model DOES generate Erdrick's-Cloud (the Tifa alt used it) — the blocker was the **uniform skeleton gate over-crediting Fusion Sword's off-element self Exploit Weakness**. **FIX: skeleton gate `scopeAware:false → true` (precise, scope+element-aware)** — off-element seeds no longer over-credited. RESULT (repro, Adaptive): Aerith-team Cloud **Fusion Sword[NonElem] → Erdrick's Sword[Lightning]**, Aerith team **172,898 → 189,392**. Runtime NET **FASTER** (tighter rankings → less downstream): Adaptive ~2.8s, Exhaustive ~22s (was ~29s). Suite green (237). **STILL OPEN:** (1) **Exhaustive BEST (222k) < Adaptive BEST (248k)** same roster — should never happen for a superset search; investigate (pre-existing vs nudged). (2) **Garb still not chosen for Aerith** — separate (owned? or Exploit Weakness already covered by attackers → genuinely redundant?). (3) the user's PRIMARY concern — **Cid-team Squeaky Hammer sub-weapon** — untouched (sub-weapon marginal-gain heuristic in `FinalizeTeamCandidate`, the Stage-2/3 territory).

**SUB-WEAPON ALLOCATION — SUBOPTIMAL (user-corrected; my "resolved" was WRONG).** After the precise gate, the greedy gave Cloud {Squeaky Hammer, Infiltration Sword, Bahamut Greatsword} and Cid {Gorgeous Staff, Mad Minute, Bahamut Cutter}. I wrongly called this fixed (Cloud has a PATK-all-allies + ability-pot weapon). **User's correction — it's wrong on TWO axes:** (1) **per-character offensive passives must be CONCENTRATED on the higher-damage attacker.** Both Bahamuts (Boost PATK All-Allies +23 AND **Boost Phys. Ability Pot.** +21 + a selfish passive) belong on **Cloud** — he's the 2nd attacker (Erdrick's + the 1.5× single-target bonus → higher weapon %), and Ability-Pot + the selfish passive scale **Cloud's own** damage (stack Ability-Pot to its breakpoint). The model SPLIT them (one each) → neither hits the per-char breakpoint on the body that matters. (2) **maximize team-wide PATK-all-allies stacks + park holder-agnostic buffs on the lowest body.** Cid (3rd/lowest weapon %) should carry Squeaky Hammer (its **Lightning Mastery All-Allies** helps regardless of holder) PLUS two MORE PATK-all-allies weapons so **Cid also hits 46pt** → party banks PATK-all-allies from BOTH Cloud and Cid. The model parked Squeaky Hammer's defensive points on the HIGHEST-damage sub-DPS (Cloud) — backwards. **ROOT = the deep de-pollution I flagged:** the damage model is **carry-centric for passives** (`carryEffectivePassives` = carry's own + others' team-wide; a non-carry's per-character passive like Cloud's Ability Pot is DROPPED), so the greedy can't see that concentrating Ability-Pot on Cloud boosts Cloud's damage; AND the team-wide passive scoring appears to **diminish after ~46pt** (didn't reach for a 3rd/4th PATK-all-allies). **FIX (deep, 2-part):** (a) **per-attacker passive handling** (each attacker's OWN passives valued on THEIR damage — the deferred part of the confinement fix), then (b) **team-damage-aware sub-weapon marginal gain** (greedy then naturally concentrates per-char passives on the higher-damage body + maximizes team-wide stacks). **OPEN MECHANIC QUESTION (awaiting user, gates the design):** do same-name **All-Allies** R-abilities from DIFFERENT providers **STACK** (Cloud 46 + Cid 46 = two buffs) or **POOL** (model currently SUMS via `AggregateTeamWidePassivePoints` → one 92pt total)? And is more PATK-all-allies ALWAYS better (higher tiers) or is there a real "enough" cap? The answer decides whether (b) also needs per-provider breakpoint tracking + a fix to the diminishing-returns curve. **STATUS: CHECKPOINT — user testing the UI + will advise on the mechanic before the deep fix.**

**✅ ELEMENT-GATING FIX LANDED (off-element over-credit) — big, user-validated impact.** Bug: `GetWeaponEffectiveDamagePercent` discounts the off-element WEAPON % (0.85 non-elem / `OffElementDamageFactor`), but the effect MULTIPLIERS were applied per-attacker with NO element gate — so a non-elemental attacker (Cloud on Fusion Sword in a Lightning fight) still got credited **Exploit Weakness** (a weakness-hit multiplier he can never trigger) + the team's elemental debuffs. Fix (DamageModel.cs): `AttackerCanHitWeakness(variant, request)` (true if any of main/off/ult deals the weakness element) + `IsElementDependentEffect(effect)` (`Key=="exploit_weakness"` or `AxisKey=="elemental"`); the per-attacker loop STRIPS element-dependent effects from any attacker that can't hit the weakness (separate `sharedOffElementCache`). The weapon-% discount stays as the BASE penalty; non-element buffs (PATK, phys/mag weapon-boost) stay. (User: materia can technically deal ~300% on-element but no real build relies on it → practically gated.) Uniform GATE path left un-gated (coarse, over-credits → includes; precise final corrects — consistent with the cheap-gate philosophy). **MEASURED IMPACT (repro, Adaptive):** BEST flipped **Cloud/Yuffie/Aerith (~249,877) → Yuffie/Cloud/Cid (247,999)**; **Cloud's main flipped Fusion Sword[NonElem] → Erdrick's Sword[Lightning]** (on-element, once the Exploit-Weakness over-credit stopped propping the off-element weapon); the Aerith team fell to a #2 alt (~172,898). This VINDICATED the user's two observations (Fusion Sword mispick + "the Cid team looks better") — the result is now a coherent all-Lightning attacker team. Suite green (237). (NOTE: a wholly off-element attacker losing exploit_weakness/elemental is a binary all-or-nothing strip — fine for the clear cases; mixed-element attackers keep everything if ANY weapon hits the weakness.)

**FOLLOW-UP GAP (banked, deferred) — attacker opportunity cost of casting buffs/debuffs + buff EXTENSION.** "You want your attackers attacking." A DPS carrying a support weapon (e.g. Yuffie's "Wind Slash" = a non-elemental weapon whose value is a Lightning Resist Down debuff) loses attack casts maintaining it; the model credits the debuff but not the lost damage. KEY asymmetry: a BUFF (Heirloom Brush tier-increase) is one-cast + main-extended (cheap); a DEBUFF (Resist Down) needs periodic RE-APPLICATION (recurring lost attacks) → debuff work belongs on a SUPPORT. Bucketed-uptime doesn't capture apply-once-vs-reapply or the opportunity cost. See memory battle-flow-atb-and-conditional-effects.

**✅ 2.6 SINGLE/ALL-TGT SIGNAL FIXED.** The earlier 2.6 keyed off `TargetScope == SingleEnemy` (which ENEMIES the debuff hits) — the WRONG signal; the Single-Tgt/All-Tgt distinction (which ATTACK TYPES benefit) is in the effect NAME and was discarded (all → `phys/mag_damage_received_up`). Fix: new `DetectedActiveEffect.AppliesOnlyToSingleTargetAttacks` flag, set `true` only for the `"Single-Tgt. … Dmg. Rcvd. Up"` detection labels (lines 137/140); it survives the detection dedup because the Single-Tgt label is added BEFORE the bare label and dedup keeps `group.First()`. The 2.6 filter (both per-attacker shared + uniform branch) now drops `AppliesOnlyToSingleTargetAttacks` effects when the carry swings an AOE weapon (`IsAllEnemiesRange`), keeping All-Tgt/bare. Removed the now-dead `IsDamageReceivedUpFamily` helper + its obsolete test; added `DamageModel_DamageReceivedUp_SingleTgtIsFlagged`. Suite green (237). (Still a carry-range approximation, not full per-attacker range-matching — acceptable.)

**UI CHOOSER FOLLOW-UP (user feedback) — Quick Copy + Debug Notes now follow the selection.** Switching teams swaps the build-out AND the Quick Copy text AND the Debug Notes. Quick Copy: hidden per-team `<textarea class="team-quickcopy-source">` sources + the chooser JS sets the single visible textarea's value (so the existing Copy button is unchanged). Debug Notes: added `DebugNotes` to `PlayerPowerAnalyzerV2AlternateTeam` (populated from the candidate's `t.DebugNotes` in the enrichment), `ChooserTeam` carries it, rendered as per-team toggle blocks. `BuildQuickCopyText` now takes `List<PlayerPowerAnalyzerV2CharacterBuild>` (was the whole result). Suite green (237).

**MEASURED IMPACT (repro, Exhaustive) — upper-bound experiment (dropped Self buffs entirely):** best team score **439k → 206k (~halved)**, and the recommended ROSTER **flipped**: Cloud/Yuffie/**Aerith** (439k) → Yuffie/Cloud/**Cid** (206k), with Aerith's team falling to a #3 alternate (127k). So self-buffs were ~half of the team-wide multiplier and were inflating the Aerith team MORE than the Cid team → uneven distortion that changes the answer, not just the magnitude. CAVEATS: (1) UPPER BOUND — dropping self-buffs entirely also removes the carry's legit owner-portion value, so the REAL confinement fix (keep each on its owner's share) shifts LESS and may not fully flip; implement + re-measure. (2) The corrected model leans Cid > Aerith (more raw damage) — the model being right about damage but blind to piloting difficulty; multi-team output (shows both) is the mitigation, and Aerith stays a close visible alternative.

## 🔭 Future phase (user-proposed) — UI TEAM CHOOSER (build-out on demand)
Now that the analyzer exposes "other teams," showing only character names + score leaves the player asking "what build is that?". **Add a UI chooser:** after the analyzer runs, the recommended team is the selected choice by default, but the user can select any of the alternate teams and see its FULL build-out (weapons/ultimate/costume/subs per character). **Service groundwork is LARGELY DONE:** `PlayerPowerAnalyzerV2AlternateTeam.CharacterBuilds` already carries each alternate's complete `PlayerPowerAnalyzerV2CharacterBuild` list (from the multi-team enrichment), plus `AddsVsBest`/`DropsVsBest` (now damage-filtered). So this is mostly a UI lift (a selector + reusing the existing build-card rendering for the chosen alternate); minor service polish only if the UI needs extra per-alternate fields. High value, additive, low backend risk. Do AFTER the scorer rework.

## Phase-1 unit-test targets (model, already green)
- Cross-family > same-family: two different-family +30% → ×1.69 beats one family +60% → ×1.60.
- Active tier→% resolves correctly: PATK Up High = 30%, Elem Damage Up High = 40%, PDEF Down High = 35%, Elem Resist Down High = 50%.
- Amplifier moves a present family +2 tiers (High 30% → Extreme High 50%) and is 0 with nothing to amplify.
